<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }}</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
  <style>
    html, body {
      height: 100%;
      margin: 0;
    }
    body {
      display: flex;
      flex-direction: column;
    }
    main {
      flex: 1;
    }
  </style>
</head>
<body class="bg-gray-100">
<header class="bg-blue-600 text-white p-4">
  <div class="container mx-auto flex justify-between items-center">
    <h1 class="text-2xl font-bold">Mon Application</h1>
    <div class="flex items-center">
      {% if user %}
        <div class="flex items-center">
          <span class="text-lg mr-2">{{ user.firstName }} {{ user.lastName }}</span>
          <form action="/auth/logout" method="POST">
            <button type="submit" class="bg-red-500 text-white rounded p-2">
              <i class="fas fa-sign-out-alt"></i> Se déconnecter
            </button>
          </form>
        </div>
      {% else %}
        <a href="/auth/login" class="bg-blue-500 text-white rounded p-2 mx-2">
          <i class="fas fa-sign-in-alt"></i> Se connecter
        </a>
        <a href="/auth/register" class="bg-blue-500 text-white rounded p-2">
          <i class="fas fa-user-plus"></i> Créer un compte
        </a>
      {% endif %}
    </div>
  </div>
</header>

<main class="container mx-auto p-4">
  {% block body %}{% endblock %}
</main>

<footer class="bg-blue-600 text-white p-4 mt-8">
  <div class="container mx-auto text-center">
    <p>&copy; 2023 Mon Application. Tous droits réservés.</p>
  </div>
</footer>
</body>
</html>
